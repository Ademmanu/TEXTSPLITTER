app = "your-app-name" # change this

[build]
  image = "registry.fly.io/your-app-name" # optional, fly will build own image if omitted

[env]
  # Set DB_PATH to the persistent mount location
  DB_PATH = "/data/botdata.sqlite3"

[[mounts]]
  source = "botdata"
  destination = "/data"